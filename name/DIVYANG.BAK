#include<stdio.h>
#include<conio.h>
main()
{
	int i,j,s;
	clrscr();
	for(i=1;i<=7;i++)
	{
	    for(j=1;j<=35;j++)
	    {
		if((i==1 && (j==3 || j==4) || (j>=11 && j<=13) || (j>=16 && j<=18)
			 || (j>=26 && j<=28) || (j==30 || j>=34 && j<=35)) ||

		((i==2 && j==2) || (j>=4 && j<=6) || (j>=8 && j<=9) || (j>=11 &&
			 j<=13) || (j>=16 && j<=18) || (j>=21 && j<=23) || (j>=26
			 && j<=28) || (j>=31 && j<=33) || (j==35 ))




		)
		{
		    printf(" ");
		}

		else
		{
			printf("*");
		}

	    }
	     printf("\n");
	}

	getch();



}